import React, { Fragment } from 'react';
import Hero from './Hero';

const Product = () => {
    return (
        <Fragment>
            <Hero />
            <Hero />
        </Fragment>
    );
};

export default Product;